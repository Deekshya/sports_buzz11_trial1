import 'package:flutter/material.dart';

class BestXIScreen extends StatelessWidget {
  const BestXIScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('bestXI'),
    );
  }
}
